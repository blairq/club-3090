# ===========================================================================
# Profile (at-a-glance):
#   Model:     Qwen3.6-27B (Lorbus AutoRound INT4 + BF16 mtp.fc preserved)
#   Topology:  Dual 3090 (TP=2, NVLink auto-detected via NVLINK_MODE)
#   Drafter:   z-lab DFlash N=5 (block-diffusion drafter)
#   KV:        FP16 (default - DFlash needs head_size=256 + non-causal attention)
#   Vision:    yes
#   Max ctx:   185K
#   Genesis:   none — DFlash drafter handles spec-decode independently
#   Status:    ✅ Production
#   Best for:  Peak code TPS with vision (AL ~4.4 vs MTP's 3.4) ⭐
# ---------------------------------------------------------------------------
# Dual-card DFlash — TP=2 + DFlash N=5 spec-decode + 185K ctx + vision.
#
# Enables DFlash cross-attention speculative decoding. Unlocks 78 narr / 128 code TPS.
# Keeps MoonViT Vision ON.
#
# Custom Dual Networks (lanbridge + hostlink) and dual_stack_proxy bindings preserved.
# ===========================================================================
# Hardware metadata (parsed by scripts/preflight.sh):
# Requires-min-vram-gb: 24
# Engine-profile: vllm-nightly-dflash
# Requires-min-gpu-count: 2
# Tensor-parallel: 2
services:
  vllm-qwen36-27b-dual:
    image: ${VLLM_IMAGE:-vllm/vllm-openai:latest${VLLM_NIGHTLY_SHA}}
    container_name: "${ESTATE_CONTAINER:-vllm-qwen36-27b-dual}"
    restart: "no"
    ports:
      - "${BIND_HOST:-0.0.0.0}:${ESTATE_PORT:-${PORT:-8020}}:8000"
    networks:
      lanbridge:
        ipv4_address: 192.168.1.222
        ipv6_address: 2803:9810:b6ea:b800::2
      hostlink:
    volumes:
      - /home/usuario/Proyectos/dual_stack_proxy.py:/app/dual_stack_proxy.py:ro
      - ${MODEL_DIR:-../../../../../models-cache}:/root/.cache/huggingface
      # torch.compile + Triton kernel caches
      - ../../cache/torch_compile:/root/.cache/vllm/torch_compile_cache
      - ../../cache/triton:/root/.triton/cache
      # Marlin pad-sub-tile-n (vLLM PR #40361)
      - ../../patches/vllm-marlin-pad/marlin.py:/usr/local/lib/python3.12/dist-packages/vllm/model_executor/kernels/linear/mixed_precision/marlin.py:ro
      - ../../patches/vllm-marlin-pad/MPLinearKernel.py:/usr/local/lib/python3.12/dist-packages/vllm/model_executor/kernels/linear/mixed_precision/MPLinearKernel.py:ro
      # vLLM PR #35936 required-tool fallback
      - ../../patches/vllm-pr35936-required-fallback/vllm/entrypoints/openai/chat_completion/serving.py:/etc/club3090/pr35936-chat-completion-serving.py:ro
      - ../../patches/vllm-pr35936-required-fallback/vllm/entrypoints/openai/engine/serving.py:/etc/club3090/pr35936-engine-serving.py:ro
      - ../../patches/vllm-pr35936-required-fallback/install.sh:/etc/club3090/install-pr35936.sh:ro
      # vLLM PR #41800 (truncate_prompt_tokens kwarg) overlay
      - ../../patches/vllm-pr41800-truncate-prompt-tokens/install.sh:/etc/club3090/install-pr41800.sh:ro
      # qwen-hybrid-chat-template qwen3.6
      - ../../patches/qwen-hybrid-chat-template/chat_template.jinja:/etc/qwen-hybrid-chat-template.jinja:ro
      # NVLink auto-detection
      - ../../../../../scripts/detect_nvlink.sh:/etc/club3090/detect_nvlink.sh:ro
    environment:
      - NVIDIA_VISIBLE_DEVICES=${ESTATE_GPUS:-${NVIDIA_VISIBLE_DEVICES:-all}}
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - VLLM_API_KEY=${VLLM_API_KEY:-}
      - VLLM_WORKER_MULTIPROC_METHOD=spawn
      - NVLINK_MODE=${NVLINK_MODE:-auto}
      - NCCL_CUMEM_ENABLE=0
      - NCCL_P2P_DISABLE=1
      - VLLM_NO_USAGE_STATS=1
      - VLLM_USE_FLASHINFER_SAMPLER=1
      - OMP_NUM_THREADS=1
      - PYTORCH_CUDA_ALLOC_CONF=${PYTORCH_CUDA_ALLOC_CONF:-expandable_segments:True,max_split_size_mb:512}
    shm_size: "16gb"
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    entrypoint:
      - bash
      - -c
      - |
        # Install PR #35936 overlay before vllm imports
        bash /etc/club3090/install-pr35936.sh
        bash /etc/club3090/install-pr41800.sh
        # NVLink auto-detection (sets NCCL env vars, _NVLINK_ENABLED).
        source /etc/club3090/detect_nvlink.sh
        # Start dual stack proxy
        python3 /app/dual_stack_proxy.py 8000 127.0.0.1 8001 &
        if [ "${_NVLINK_ENABLED:-0}" = "1" ]; then
          exec vllm serve ${VLLM_ENFORCE_EAGER:+--enforce-eager} "$@"
        else
          exec vllm serve ${VLLM_ENFORCE_EAGER:+--enforce-eager} --disable-custom-all-reduce "$@"
        fi
      - --
    command:
      - --model
      - /root/.cache/huggingface/qwen3.6-27b-autoround-int4
      - --served-model-name
      - qwen3.6-27b-autoround
      - --quantization
      - auto_round
      - --dtype
      - bfloat16
      - --tensor-parallel-size
      - "${TP:-2}"
      - --pipeline-parallel-size
      - "${PP:-1}"
      - --max-model-len
      - "${MAX_MODEL_LEN:-185000}"
      - --gpu-memory-utilization
      - "${GPU_MEMORY_UTILIZATION:-0.95}"
      - --max-num-seqs
      - "1"
      - --max-num-batched-tokens
      - "8192"
      # No --kv-cache-dtype: DFlash needs head_size=256 + non-causal attention,
      # and no Ampere backend supports that triple with fp8/turbo KV. FP16 default.
      - --trust-remote-code
      # qwen-hybrid chat-template override (see volumes block + docs/UPSTREAM.md)
      - --chat-template
      - /etc/qwen-hybrid-chat-template.jinja
      - --reasoning-parser
      - qwen3
      - --default-chat-template-kwargs
      - '{"enable_thinking": false}'
      - --enable-auto-tool-choice
      - --tool-call-parser
      - qwen3_coder
      - --enable-prefix-caching
      - --enable-chunked-prefill
      - --speculative-config
      - '{"method":"dflash","model":"/root/.cache/huggingface/qwen3.6-27b-dflash","num_speculative_tokens":5}'
      - --host
      - "127.0.0.1"
      - --port
      - "8001"

networks:
  lanbridge:
    external: true
  hostlink:
    driver: bridge
    enable_ipv6: true
    ipam:
      driver: default
      config:
        - subnet: 172.29.0.0/16
        - subnet: fd00:beef::/64
